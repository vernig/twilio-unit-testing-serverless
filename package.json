{
  "name": "twilio-unit-testing-serverless",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "jest": "^26.0.1",
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "10.17.0"
  }, 
  "jest": {
    "testEnvironment": "node"
  }
}
